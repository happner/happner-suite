{
  "name": "happn-db-provider-loki",
  "version": "1.2.2",
  "description": "Loki data provider for happn",
  "scripts": {
    "test": "_mocha --recursive --exit silence.js test/unit/* test/integration/* test/prototypes/*",
    "test-cover": "nyc --reporter=lcov --reporter=text --reporter=html npm test",
    "test-unit": "mocha silence.js test/unit/* --recursive"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "adm-zip": "^0.5.9",
    "happn-commons": "1.5.1",
    "lokijs": "^1.5.12",
    "pretty-bytes": "^5.6.0"
  },
  "devDependencies": {
    "happn-commons-test": "1.2.2"
  }
}
