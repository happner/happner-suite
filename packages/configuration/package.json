{
  "name": "configuration",
  "version": "0.0.1",
  "description": "happner-3, happn-cluster, happner & happner-cluster configuration utilities",
  "main": "index.js",
  "scripts": {
    "compile": "tsc --build --clean && tsc",
    "test": "mocha --require ts-node/register 'test/**/**/**/*.ts'",
    "test-cover": "nyc --reporter=lcov --reporter=text --reporter=html npm run test"
  },
  "author": "leebow",
  "license": "ISC",
  "devDependencies": {
    "@types/chai": "^4.3.4",
    "@types/mocha": "^10.0.0",
    "@types/node": "^18.11.9",
    "chai": "^4.3.7",
    "mocha": "^10.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "acorn": "^8.8.0",
    "ajv": "^8.11.0"
  }
}
